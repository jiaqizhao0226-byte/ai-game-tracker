# AI+游戏产品情报看板：Codex 工作约定

## 项目目标

维护一个持续更新的 AI+游戏产品情报看板。项目既是产品数据库，也是用于形成行业判断的研究工具；事实准确性优先于收录速度和观点数量。

线上地址：https://jiaqizhao0226-byte.github.io/ai-game-tracker/

## 开始任务前

依次阅读：

1. `STATUS.md`
2. `NEXT_TASKS.md`
3. `CONTEXT/CLAUDE_HANDOFF.md`
4. `docs/SCHEMA.md`

不要把 Claude 原始 JSONL 整体加载进上下文；仅在交接文件无法回答具体历史问题时，按关键词检索对应 session。

## 数据与研究纪律

- 录入、分类和正文结构以 `docs/SCHEMA.md` 为唯一规范。
- 不臆造数据。无法核实的内容写「暂无公开数据」或留空。
- 区分事实、用户明确确认的信息、合理推断和待验证假设。
- 「策略启示」必须由已知事实推出，避免绝对化和过度解读。
- 不删除停运或失败产品，它们是研究样本。
- 不出现「光子策略分析组」。
- API key、登录凭据和私密信息不得写入仓库或交接文档。

## 修改原则

- 数据主要位于 `src/data.json`，更新记录位于 `src/changelog.json`。
- 产品分类变化需同步检查筛选项、统计图和 `docs/SCHEMA.md`。
- 补录老产品时注意 `sort_weight`，避免仅因新录入而占据前排。
- `featured`（重点关注）只能由用户明确指定；不得因研究价值、优先级或个人判断自行添加。
- 保留用户已确认的分类和归属，除非有更强证据；如需推翻，先说明证据冲突。
- 保持当前视觉系统，避免无关的大规模重构。

## 验证

完成修改后至少运行：

```bash
npm run build
node scripts/audit.js
```

涉及页面交互或视觉变化时，还要在本地页面检查桌面端与窄屏，并确认：

- 首页筛选、排序和返回位置正常
- 产品详情及截图画廊正常
- 概览统计与数据一致
- GitHub Pages `basePath` 下的资源路径正确
- 动画尊重 `prefers-reduced-motion`

未经用户明确要求，不推送、不部署、不修改 GitHub 远端。
